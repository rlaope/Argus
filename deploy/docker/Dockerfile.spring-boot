FROM eclipse-temurin:21-jre-alpine
WORKDIR /app
COPY build/libs/app.jar /app/app.jar

# Spring Boot starter auto-configures Argus — no -javaagent needed
# Just add argus-spring-boot-starter to your build.gradle dependencies

EXPOSE 8080 9202
ENTRYPOINT ["java", "-jar", "/app/app.jar"]
